from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Create recomm system based on cc transactional data of several users',
    author='Fariha Baloch',
    license='MIT',
)
